<Window x:Class="Expenses.AddGroceryList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Expenses"
        mc:Ignorable="d"
        Title="AddGroceryList" Height="200" Width="300" MinWidth="300" MinHeight="200">
    <Grid>
        <DataGrid x:Name="dataGrid" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="0,0,68,10" AlternatingRowBackground="#FFE9E9E9" CanUserAddRows="True"/>
        <Button Content="Save" Margin="0,0,17,66" HorizontalAlignment="Right" Width="30" Click="Save" Height="20" VerticalAlignment="Bottom"/>
        <Button Content="Cancel" Margin="0,0,13,32" IsCancel="True" Click="Cancel" HorizontalAlignment="Right" Width="38" Height="20" VerticalAlignment="Bottom"/>
        <Button Content="+" Margin="0,25,24,0" VerticalAlignment="Top" HorizontalAlignment="Right" Width="16" Click="AddRow"/>
        <Button Content="-" Margin="0,50,24,0" VerticalAlignment="Top" HorizontalAlignment="Right" Width="16" Click="RemoveRow"/>

    </Grid>
</Window>
